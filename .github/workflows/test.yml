name: Test

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  test:
    uses: jakakokosar/orange-ci-cd/.github/workflows/test-addons.yml@remove_tox_pip_upgrade
